# AImind - AI思维导图工具

> AI驱动的跨平台思维导图工具，支持智能自动完善和多AI服务切换

## 特性

- **🤖 AI Agent自动完善** - AI自主分析并智能扩展整个思维导图
- **🔄 多AI配置管理** - 类似Cursor，支持OpenAI、DeepSeek、Claude、Ollama等
- **📝 传统思维导图** - 节点创建、编辑、连接、样式、布局
- **💻 单文件便携** - 单个可执行文件，无需安装
- **🔒 数据隐私安全** - 本地存储，可选本地AI模型

## 快速开始

### 前置要求

- Node.js 18+
- Rust 1.70+ (用于打包桌面应用)

### 安装依赖

```bash
npm install
```

### 开发模式

```bash
# 启动前端开发服务器（web-only测试）
npm run dev

# 启动Tauri开发模式（完整桌面应用）
npm run tauri:dev
```

### 构建发布版本

```bash
# 构建所有平台
npm run tauri:build

# 仅构建Windows
npm run tauri:build -- --target x86_64-pc-windows-msvc

# 仅构建macOS Intel
npm run tauri:build -- --target x86_64-apple-darwin

# 仅构建macOS Apple Silicon
npm run tauri:build -- --target aarch64-apple-darwin
```

### 类型检查

```bash
tsc
```

## 使用说明

### 基本操作

| 快捷键 | 操作 |
|--------|------|
| Tab | 新建子节点 |
| Enter | 新建兄弟节点 |
| Space / 双击 | 编辑节点 |
| Delete | 删除节点 |
| / | 折叠/展开节点 |
| Ctrl + S | 保存 |
| Ctrl + J | AI扩展选中节点 |
| Ctrl + Shift + A | AI Agent自动完善 |

### AI配置

1. 点击工具栏右侧的AI服务选择器
2. 选择"管理配置"
3. 添加或编辑AI服务配置
4. 输入API密钥（Ollama本地服务无需密钥）
5. 保存并设为默认

### AI Agent使用

1. 创建一个新的思维导图
2. 点击右侧AI面板的"AI Agent自动完善"按钮
3. AI将分析主题并生成完整思维导图

## 项目结构

```
AImind/
├── src-tauri/       # Tauri后端 (Rust)
├── src/             # React前端
│   ├── components/  # UI组件
│   ├── stores/      # 状态管理
│   ├── hooks/       # 自定义Hooks
│   └── lib/         # 工具库
├── public/          # 静态资源
└── docs/            # 文档
```

## 技术栈

- **前端**: React 18 + TypeScript + Vite + TailwindCSS
- **跨平台**: Tauri 2.x
- **状态管理**: Zustand
- **思维导图**: 自定义SVG画布（非React Flow）
- **AI集成**: OpenAI API兼容接口

## 核心架构

### 自定义画布实现

本项目实现了**自定义思维导图画布**，而非使用React Flow库：

```
MindMapCanvas (src/components/MindMapCanvas.tsx)
├── SVG Layer - 渲染连接线（三次贝塞尔曲线）
└── Node Layer - 递归渲染MindMapNode组件
```

**核心文件**：
- [`MindMapCanvas.tsx`](src/components/mindmap/MindMapCanvas.tsx) - 主画布容器，支持拖拽/缩放
- [`MindMapNode.tsx`](src/components/mindmap/MindMapNode.tsx) - 单个节点组件
- [`src/lib/mindmap/layout.ts`](src/lib/mindmap/layout.ts) - 布局引擎（水平/垂直）

**画布功能**：
- 拖拽：鼠标拖动移动画布
- 缩放：Ctrl + 鼠标滚轮
- 可折叠节点：切换子节点显示
- 自动布局：基于树结构自动定位节点

### 状态管理（Zustand）

应用使用**三个独立的Zustand store**管理不同领域：

1. **`src/stores/mindmapStore.ts`** - 核心思维导图数据
   - `mindmap`: 所有节点的树结构
   - `selectedNodeId`: 当前选中节点
   - 节点CRUD操作、树遍历、布局管理
   - AI节点生成方法

2. **`src/stores/configStore.ts`** - 应用配置和UI状态
   - `appConfig`: 主题、自动保存、最近文件
   - `ui`: 面板状态、缩放级别、侧边栏可见性
   - 主题管理、缩放控制

3. **`src/stores/aiStore.ts`** - AI提供商配置
   - `config`: 多个AI提供商配置（OpenAI、DeepSeek、Claude、Ollama、自定义）
   - `currentProvider`: 当前激活的AI服务
   - `messages`: 聊天历史
   - `agentConfig`: AI Agent行为设置（深度、广度、扩展规则）

**重要**：所有状态更新都是不可变的（使用展开运算符）。Store使用选择器获取派生状态。

### AI集成

AI系统通过抽象层支持多个提供商：

**提供商类型**：OpenAI、DeepSeek、Anthropic (Claude)、Ollama（本地）、自定义

**核心文件**：
- [`src/lib/ai/provider.ts`](src/lib/ai/providers.ts) - 提供商API实现
- [`src/lib/ai/agent.ts`](src/lib/ai/agents.ts) - AI智能自动完善
- [`src/hooks/useAI.ts`](src/hooks/useAI.ts) - AI操作的React Hook

**AI功能**：
1. **直接节点扩展** (Ctrl + J)：为选中主题生成子节点
2. **AI Agent自动完善** (Ctrl + Shift + A)：分析和扩展整个思维导图
3. **聊天界面**：右侧面板中的对话AI助手

**Agent行为**：
- 基于优先级的扩展（根据深度/广度决定哪些节点需要扩展）
- 可配置的深度限制和扩展规则
- 操作期间的进度跟踪
- 上下文感知（理解思维导图结构和领域）

## 数据结构

节点遵循以下结构：
```typescript
interface MindMapNode {
  id: string;
  content: string;
  children: MindMapNode[];
  position: { x: number; y: number };
  collapsed: boolean;
  // ... 其他元数据字段
}
```

树结构递归渲染。每个节点存储由布局引擎计算的位置。

## 开发注意事项

1. **自定义画布**：不要直接使用React Flow - 这是手动布局和渲染的自定义实现。

2. **状态更新**：在Zustand store中始终使用不可变更新（展开运算符）。

3. **AI提供商**：添加新AI提供商时，在[`src/lib/ai/provider.ts`](src/lib/ai/providers.ts)中实现提供商接口。

4. **布局更改**：对布局逻辑的修改应在[`src/lib/mindmap/layout.ts`](src/lib/mindmap/layout.ts)中进行。

5. **节点定位**：位置由布局引擎计算，而非手动设置。树结构的更改会触发重新计算。

## 许可证

MIT License

## 致谢

- [Tauri](https://tauri.app/) - 跨平台桌面应用框架
- [TailwindCSS](https://tailwindcss.com/) - CSS框架
